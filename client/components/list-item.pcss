@import '../style/vars.css';

.listitem {
  position: relative;
  height: 30px;
  padding: 0 6px;
  line-height: 30px;
  transition: background 300ms ease-in-out, color 300ms ease-in-out;
  cursor: pointer;

  &:hover {
    background-color: rgba(var(--wheat), 0.8);
  }
}